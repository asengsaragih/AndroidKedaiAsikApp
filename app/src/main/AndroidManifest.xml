<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suncode.kedaiasik">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HistoryActivity"></activity>
        <activity
            android:name=".TransactionActivity"
            android:label="Detail Order"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity android:name=".StoreActivity"></activity>
        <activity
            android:name=".FormMenuActivity"
            android:label="@string/menu_activity"
            android:launchMode="singleTop"
            android:parentActivityName=".OurStoreActivity" />
        <activity
            android:name=".RegisterStoreActivity"
            android:label="@string/register_store"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".OurStoreActivity" />
        <activity
            android:name=".SigninActivity"
            android:label="@string/signin" />
        <activity
            android:name=".SignupActivity"
            android:label="@string/signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>